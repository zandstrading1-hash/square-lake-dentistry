# STATUS

_As of commit `2dbeb7a` on `main`. Update this when meaningful state changes._

## Live
- ✅ Published and live: https://zandstrading1-hash.github.io/square-lake-dentistry/
- ✅ GitHub Pages enabled (branch `main`, folder `/`); last build succeeded.
- ✅ Repo public: https://github.com/zandstrading1-hash/square-lake-dentistry
- ✅ Working tree clean; local `main` == `origin/main`.

## Recent history (most recent first)
- `2dbeb7a` content: remove unverified claims, use real patient reviews (integrity/accuracy pass)
- `1175983` Add legal pages + accessibility (WCAG 2.1 AA) polish
- `d098c63` Build full multi-page site; switch to call-to-book
- `d1f80dd` Initial site (homepage, SEO schema, responsive)

## What "done" looks like right now
- 25 pages live with accurate, verifiable content.
- 6 real, attributed Google reviews for Dr. Hanna (home shows 3; reviews page shows all 6).
- No star rating / `aggregateRating` schema (removed — public sources conflicted).
- Insurance page uses generic "call to verify coverage" language.

## Known gotcha
- Pages here is the **legacy branch build**: a **force-push does NOT auto-trigger a rebuild**.
  Normal commits/pushes rebuild automatically. After a force-push, trigger a build (see COMMANDS.md).
